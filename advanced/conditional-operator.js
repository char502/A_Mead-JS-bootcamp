// const myAge = 35;
// const message = myAge >= 18 ? "You can vote" : "You are too young to vote";
// console.log(message);

// const myAge = 10;
// const showPage = () => {
//   return "showing the page";
// };
// const showErrorPage = () => {
//   return "showing the error page";
// };

// const result = myAge >= 21 ? showPage() : showErrorPage();
// console.log(result);

const team = ["Tyler", "Porter", "Mason", "Davis"];

console.log(
  team.length <= 4 ? `Team size ${team.length}` : "Too many people in your team"
);
