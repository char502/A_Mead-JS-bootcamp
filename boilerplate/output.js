// command for babel - babel input.js -o output.js
// babel preset - need to define a preset so babel knows what to do with the code you give it

const name = "char";
console.log(name);

class Hangman {
  myMethod() {
    return "Testing";
  }
}
